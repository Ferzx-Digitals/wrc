---
import PageLayout from '../../layouts/PageLayout.astro';
import Button from '../../components/ui/Button.astro';
import { CONTACT_EMAIL } from '../../utils/constants';
import { getLocaleFromUrl } from '../../i18n/utils';
import { fetchFaqItems } from '../../utils/google-sheets';

const locale = getLocaleFromUrl(Astro.url);
const prefix = `/${locale}`;

const faqItems = await fetchFaqItems('en');

const categories = [
  { id: 'all', label: 'All Questions' },
  { id: 'registration', label: 'Registration' },
  { id: 'accommodation', label: 'Accommodation' },
  { id: 'general', label: 'General' },
  { id: 'payment', label: 'Payment' },
  { id: 'sponsorship', label: 'Sponsorship' },
];
---

<PageLayout title="Frequently Asked Questions" description="Find answers to common questions about the 11th IRF World Ranger Congress — registration, accommodation, travel, payments, and more.">
  <!-- Hero -->
  <section class="relative py-16 sm:py-20 bg-gradient-to-r from-wrc-green-900/90 to-wrc-green-800/70">
    <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl sm:text-5xl font-heading font-extrabold text-white mb-4">
        How Can We Help You?
      </h1>
      <p class="text-lg text-white/80 mb-8 max-w-2xl mx-auto">
        Find answers to the most common questions about the Congress
      </p>
      <!-- Search Bar -->
      <div class="max-w-xl mx-auto">
        <div class="relative">
          <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          <input
            type="text"
            id="faq-search"
            placeholder="Search questions..."
            class="w-full pl-12 pr-4 py-4 rounded-xl text-gray-900 bg-white shadow-lg focus:outline-none focus:ring-2 focus:ring-wrc-green-500"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- 21.png decorative strip -->
  <div class="relative overflow-hidden">
    <img src="/images/home/22.png" alt="" aria-hidden="true" class="absolute -right-16 top-0 h-full w-auto opacity-15 pointer-events-none" />

  <!-- FAQ Content -->
  <section class="relative py-16">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <!-- Category Filters -->
      <div class="flex flex-wrap gap-2 mb-8" id="faq-categories">
        {categories.map((cat) => (
          <button
            data-category={cat.id}
            class:list={[
              'px-4 py-2 rounded-full text-sm font-heading font-semibold transition-colors',
              cat.id === 'all'
                ? 'bg-wrc-green-700 text-white'
                : 'bg-gray-100 text-gray-600 hover:bg-gray-200',
            ]}
          >
            {cat.label}
          </button>
        ))}
      </div>

      <!-- FAQ Items -->
      <div id="faq-list" class="columns-1 md:columns-2 gap-4" role="list">
        {faqItems.map((item) => (
          <details class="group border border-gray-200 rounded-lg overflow-hidden faq-item break-inside-avoid mb-4" data-category={item.category} role="listitem">
            <summary class="flex items-center justify-between cursor-pointer px-5 py-4 font-heading font-semibold text-gray-800 hover:bg-gray-50 transition-colors list-none">
              <span class="pr-4">{item.question}</span>
              <svg
                class="w-5 h-5 shrink-0 text-gray-400 transition-transform duration-200 group-open:rotate-180"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="px-5 pb-4 text-gray-600 leading-relaxed text-sm [&_a]:text-wrc-green-700 [&_a]:font-semibold [&_a]:underline" set:html={item.answer}>
            </div>
            {item.button_text && item.button_href && (
              <div class="px-5 pb-4">
                <a
                  href={item.button_href}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-flex items-center gap-2 px-4 py-2 text-sm font-heading font-semibold rounded-lg bg-wrc-green-700 text-white hover:bg-wrc-green-800 transition-colors"
                >
                  {item.button_text}
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                  </svg>
                </a>
              </div>
            )}
          </details>
        ))}
      </div>

      <!-- No Results -->
      <div id="faq-no-results" class="hidden text-center py-12">
        <p class="text-gray-500 text-lg">No questions match your search.</p>
        <p class="text-gray-400 text-sm mt-2">Try different keywords or browse by category.</p>
      </div>
    </div>
  </section>

  <!-- Contact CTA -->
  <section class="py-16 bg-gray-50 text-center">
    <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-heading font-bold text-wrc-green-900 mb-4">
        Still have questions?
      </h2>
      <p class="text-gray-600 mb-6">
        Can't find what you're looking for? Reach out to our team directly.
      </p>
      <div class="flex flex-wrap justify-center gap-4">
        <Button href={`${prefix}/contact`} variant="primary">
          Contact Us
        </Button>
        <Button href={`mailto:${CONTACT_EMAIL}`} variant="outline">
          Email Us
        </Button>
      </div>
    </div>
  </section>
  </div>
</PageLayout>

<script>
  const searchInput = document.getElementById('faq-search') as HTMLInputElement;
  const faqItems = document.querySelectorAll('.faq-item');
  const categoryButtons = document.querySelectorAll('#faq-categories button');
  const noResults = document.getElementById('faq-no-results');
  let activeCategory = 'all';

  function filterFAQs() {
    const query = searchInput.value.toLowerCase().trim();
    let visible = 0;

    faqItems.forEach((item) => {
      const el = item as HTMLElement;
      const category = el.dataset.category || '';
      const text = el.textContent?.toLowerCase() || '';
      const matchesCategory = activeCategory === 'all' || category === activeCategory;
      const matchesSearch = !query || text.includes(query);
      const show = matchesCategory && matchesSearch;
      el.style.display = show ? '' : 'none';
      if (show) visible++;
    });

    if (noResults) {
      noResults.classList.toggle('hidden', visible > 0);
    }
  }

  searchInput?.addEventListener('input', filterFAQs);

  categoryButtons.forEach((btn) => {
    btn.addEventListener('click', () => {
      activeCategory = (btn as HTMLElement).dataset.category || 'all';
      categoryButtons.forEach((b) => {
        const isActive = (b as HTMLElement).dataset.category === activeCategory;
        b.classList.toggle('bg-wrc-green-700', isActive);
        b.classList.toggle('text-white', isActive);
        b.classList.toggle('bg-gray-100', !isActive);
        b.classList.toggle('text-gray-600', !isActive);
        b.classList.toggle('hover:bg-gray-200', !isActive);
      });
      filterFAQs();
    });
  });
</script>
